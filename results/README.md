# Generated Results

Experiment runs write detailed CSV and JSON summaries to this directory. These generated files are ignored by Git.

Compact final-report tables are retained under `artifacts/results/`.
