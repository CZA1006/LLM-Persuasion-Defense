name: sast-ir
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - matplotlib
  - pandas
  - seaborn
  - tqdm
  - datasets
  - pip
  - pip:
      - openai>=1.40.0,<3
      - python-dotenv>=1.0.1
      # Version used for the December 2025 baseline implementation.
      - pyrit==0.10.0
