
---

## `env.yml`
```yaml
name: llm-persuasion-defense
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - scikit-learn
  - jupyter
  - matplotlib
  - datasets
  - pip
  # CPU PyTorch default; if you have GPU, replace the three lines below with CUDA build (see comment)
  - pytorch
  - torchvision
  - torchaudio
  - pip:
      - transformers>=4.44.0
      - openai>=1.40.0
      - python-dotenv>=1.0.1
      - tiktoken
      - accelerate
      - pyrit
# For GPU users, consider:
#  conda install pytorch pytorch-cuda=12.1 torchvision torchaudio -c pytorch -c nvidia
