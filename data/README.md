# Dataset

`counterfact_50_strict.jsonl` is the final 50-example COUNTERFACT-Strict subset used in the report. Other downloaded or intermediate datasets in this directory are ignored by Git.

Regenerate the subset with:

```bash
python tools/prepare_counterfact.py
```
