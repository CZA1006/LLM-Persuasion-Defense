# Generated Traces

Experiment runs write JSONL telemetry to this directory. These files are generated artifacts and are ignored by Git to keep the repository compact.

The two report case studies retained for review are available under `examples/traces/`.
